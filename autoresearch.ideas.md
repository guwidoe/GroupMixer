# Solver3 construction autoresearch ideas backlog

## Current post-merge active diagnostics

- Current source HEAD `a90fa2c8` (`perf(solver3): use dynamic oracle target acceptance`) is the best observed run at `0.987980271`, but no-code repeats scored around `1.16` and returned the zero guard; treat it as promising but unstable rather than robust.
- Investigate the zero-baseline guard regression on `stretch.benchmark-very-large-constrained` generically; many otherwise-good construction basins still produce small nonzero final scores there.
- Focus first on generic constructor/search-basin interactions visible in Google-CP equivalent, transfer attribute balance, large gender immovable, constrained 169x13x14 SGP, and Sailing. Do not add case-ID-specific logic or benchmark-case edits.
- Treat the hard-apart constructor-ownership change as provisionally safe for this lane: it improved the post-merge parent and kept `35/35` cases passing.

## Preferred structural experiments

- Implement an oracle-guided baseline fill rather than overwrite+repair only with real objective-aware placement; oracle seed + legacy random baseline fill was not enough. A greedy targeted-displacement merge that avoided full selected-group clearing and evicted per import regressed to `1.136574038`, and simple source-group pair-pressure loss in target acceptance regressed to `1.159267089`, so any fill redesign needs holistic assignment rather than local evictions or local donor-loss subtraction.
- `perf(solver3): skip oracle for attribute-only contact scenarios` (`4fa9ab9e`) was superseded by `a90fa2c8 perf(solver3): use dynamic oracle target acceptance`: the new line removes the blanket attribute-only scaffold skip and instead lets oracle merge run with dynamic target acceptance scored against already accepted group members. First run improved the best observed primary to `0.987980271` with `zero_regression_count=0`; no-code repeats regressed to `1.163056555` and `1.156862610` with the zero guard back, so treat it as best observed but highly unstable. The old skip first scored `1.020929244` but repeated at `1.059330696` and `1.113111594`; reapplying the blanket skip on top of dynamic target acceptance repeated at `1.107906075`, so it is not a stability fix. Narrowing the skip to group-scoped balances regressed to `1.147266210`, extending it to soft-pair scenarios regressed to `1.070419641`, bounded missing-contact swap polish regressed to `1.135818363`, removing the repeat-free restriction regressed to `1.155204086`, and reverting the skip alone regressed to `1.156025716`. A score gate (`scaffold_score > num_people`) eliminated the zero guard but regressed primary to `1.048522816`; a near-perfect residual gate (`scaffold_score > 1.0`) regressed to `1.088635709`; a complete-uniform-capacity exception regressed to `1.180971995`; a baseline-relative 5% score gate regressed to `1.111506200`. On the dynamic line, the opposite low-residual scaffold guard (`scaffold_score <= 2`) regressed to `1.183538500`, and reusing the num-people high-residual scaffold gate regressed to `1.148736343`; residual-threshold guards remain unhelpful.
- Keep deterministic batch assignment for displaced merge repair after the conservative original-slot restore pass; restoring displaced people to their original group when oracle imports leave open capacity is now the best kept line. Do not sort restore candidates by pair/placement score, lower the repair prior, preserve same-group accepted occupants in place, gate restore off for clique sessions, stage zero-displacement oracle moves before clearing, constrain remaining repair to selected-region holes, add a soft selected-region repair prior, force-fill outside source vacancies before repair, tune selected-region target acceptance bonus away from 0.5 (0.0 and 1.0 both regressed), or make hard-apart DFS repair search for best local repair score instead of first feasible; those all regressed.
- Re-evaluate small, principled scaffold-disruption weights only if paired with a structural candidate-risk/fill-order change; simple scalar retuning has had diminishing returns. A flexible-occupant disruption risk of 0.25 eliminated the zero guard and scored 1.056 but regressed successful-case mean versus the kept best; 0.10 was worse. Attribute-only 0.25 flexible risk produced excellent successful-case means (1.025 then 1.011) but repeatedly returned one zero-regression penalty. Retrying plain 0.25 flexible risk on the current attribute-skip baseline regressed to `1.133460122`; structural-only conditioning regressed and repeated with failures. Do not keep flexible-risk variants unless the zero guard is solved generically and repeated.
- Evaluate multiple top template candidates only with strong hard-feasibility/risk/contact tradeoffs; naive top-3 projection-score and lowest-disruption-frontier selectors were both negative. A top-two actual merge-displacement-risk selector failed checks and catastrophically regressed (`1001.722331826`, one failure, four zero regressions, much higher construction time), so multi-candidate selection needs much stronger safeguards before revisiting.
- Window-length tuning is not stable enough to keep: requiring oracle template windows to span at least three sessions first scored `1.022566492` with no zero guard, but repeat regressed to `1.097158170`; a softer two-session score penalty regressed to `1.051442141` and returned the zero guard, combining three-session minimum with the attribute score gate regressed to `1.163873440`, and full-horizon-only templates regressed to `1.093851657`. On the dynamic line, full-horizon filtering only for attribute-only contact scenarios fixed the guard and scored `1.036785986`, but repeat regressed to `1.088018404` despite keeping the guard fixed; broadening that idea to horizon-edge templates regressed to `1.128878889` and returned the guard; combining full-horizon filtering with keep=4 regressed to `1.102646470` and returned the guard; narrowing that filter to complete-uniform attribute/contact designs regressed to `1.138108089` and returned the guard. A capacity-structure split (surplus-capacity attribute/contact cases return scaffold; tight cases use full-horizon templates) had good mean but still scored `1.072683271` with a zero penalty; exact full-coverage template preference regressed to `1.171067845`; combining the capacity split with two projection iterations regressed to `1.106255853`. Pairing the capacity split with a stronger keep bonus produced one `1.032219796` zero-guard run with good transfer/flotilla, but repeat regressed to `1.171683590` with the guard back; do not treat it as stable. Use telemetry before more window-length tuning; horizon-edge and longest-near-best attribute-only selectors also regressed (`1.128878889` and `1.130355822`).
- Add benchmark telemetry for oracle outcome/template dimensions per case to understand whether regressions come from candidate choice, projection, merge displacement, or hard-constraint validation. A hard coherent cross-session group-ID selector regressed to `1.186163164`; if region coherence is revisited, use telemetry/soft scoring rather than forcing the same group indices across the whole template.
- Keep projection assignment/alignment iterations at 1 for the committed line. Extra convergence passes worsened both before and after batch repair despite some sentinel gains, and zero iterations also regressed (`1.132499135`). On the dynamic line, two projection iterations produced one excellent successful-case mean (`0.973070532`) but still scored `1.042507717` because the zero guard remained; repeat regressed to `1.130554180`, combining two iterations with attribute-only full-horizon filtering fixed the guard but regressed mean to `1.087851792`, splitting the combination so extra iterations only affect non-attribute-only cases still regressed to `1.158730261`, and combining two iterations with the blanket attribute scaffold skip regressed to `1.140961677`; a simple rigid-anchor projection seed bonus regressed to `1.127749084`. Do not keep/retry projection anchoring signals without new telemetry.
- Avoid scaffold-cohort pressure in repair; positive and negative cohort terms after batch assignment were both basin-hostile.
- Do not continue scalar tuning of batch repair's preferred-group prior; 1.0 and 3.0 both worsened versus kept 2.0, and 1.0 still regressed after original-slot restore.
- Do not combine hard selected-region target priority with batch repair; it was much worse than with greedy repair.
- Do not revisit available-capacity-only template sizing or available-capacity-first slot selection; sizing failed and slot ordering regressed transfer/49x7 despite feasible output.
- Do not continue attendance scalar tuning in projection/template selection or target acceptance; high-attendance hard filter, high-attendance threshold 2/3, absent-penalty 0.5, and a target-acceptance participation bonus all worsened. Raising stable-coverage template weight to 0.50 produced strong successful-case means in some runs but repeatedly returned the zero guard and regressed when combined with lower outside-import penalty, so it is not keepable without a generic guard fix.
- Keep target acceptance placement-frequency at the default weight; lowering it to 0.5 regressed to `1.071913953`, raising it to 2.0 regressed to `1.094987108`, and retrying 0.5 on the dynamic line regressed to `1.125019441`. Keep dynamic target acceptance from `a90fa2c8` for now, but treat it as noisy: scoring candidates against already accepted group members only became keepable when paired with removing the blanket attribute-only scaffold skip, first improving primary to `0.987980271` and zeroing the guard, then repeating at `1.163056555` with the guard back. The same dynamic target-acceptance change alone scored `1.038609598`; conditioning it only outside hard-apart/non-displacing sessions regressed to `1.072962291`; using static target ordering whenever repeat pressure exists regressed to `1.146378514`; gating dynamic acceptance only to attribute-constrained cases regressed to `1.131517121`; a scaffold+accepted-member pressure blend scored `1.048163856` with the zero guard, seeding dynamic pressure from the scaffold only while the accepted group is empty regressed to `1.141375424`, and using projected-oracle-order tie-breaking regressed to `1.099875091`. Removing or halving pair pressure improved Sailing but worsened broad balance; halving the dynamic accepted-member pair-pressure term also regressed to `1.100365514`, so avoid more simple pair-pressure blends without telemetry.
- Keep projected oracle target groups coherent; global cross-target acceptance assignment and pre-reserving local slots before oracle target acceptance both destroyed pure/constrained SGP structure.
- Avoid oracle-cohort pressure in target acceptance; max(scaffold target pressure, oracle co-target pressure) worsened broad quality and constrained SGP 169x13x14, and seed-only oracle-cohort pressure for initially empty dynamic groups regressed to `1.114516209`.
- Keep projection contact-signature anchoring at 0.20; 0.15, 0.25, and 0.30 all regressed broad balance, so stop scalar tuning this term.
- Keep scaffold-disruption template penalty at 3.0; lower 2.5 remained worse even with batch repair.
- Keep the 1s full-objective warmup scaffold with the 1,000,000 iteration cap; skipping warmup caused a zero-baseline regression, skipping only when the pre-warmup baseline already scores zero still regressed, replacing the pre-warmup baseline source with freedom-aware construction caused multiple failures, lowering the warmup cap to 250,000 on the dynamic line regressed to `1.163402831`, and removing wall-time enforcement for a deterministic 500k-iteration warmup caused 17 benchmark failures (`17000.482548038`).
- Explore adaptive warmup/oracle seeding only with a fundamentally different signal (not just budget size, seed salting, blind multistart, or swapping the warmup improver); blind 2s, budget-guarded 2s, baseline/warmup consensus rigidity, using the unsalted effective seed for warmup, unsalting the pure oracle seed (including retrying it on the dynamic line at `1.125952896`), two-seed warmup for attribute-only contact skip, residual-triggered extra polish for near-perfect attribute-only scaffolds (`1.103797063`), globally switching warmup to SGP week-pair tabu (`checks_failed`, 10 failed benchmark cases), and repeat-only SGP tabu warmup (`1.109470200`) were negative.
- Benchmark/search noise is material: no-code repeats of kept lines scored much worse, including the original-slot restore line repeating at `1.084922934` versus kept `1.03507645`. Rerun near-threshold candidates before keeping if confidence is unclear.

## De-emphasized / recently negative

- Whole-problem pure-SGP fast path before scaffold/template logic: explicitly rejected.
- User-facing projection/oracle knobs: not allowed; behavior must be automatic.
- Score-gated oracle acceptance or hidden fallback after merge/repair claims applicability: not allowed during this strict constructor phase.
- More aggressive scaffold-disruption penalty at 5x under the old log metric: worsened versus the 3x keep.
- More permissive disruption penalty at 2x/2.5x under the old log metric: improved Sailing/raw total in some runs but worsened old aggregate and constrained SGP balance.
- Naive surgical merge that avoids selected-region clearing: worsened relative score and introduced a zero-regression.
- Oracle seed + legacy baseline refill: faster and feasible but weaker than direct merge; full fill needs objective-aware placement, not random baseline fill.
- Scaffold backfill for dummy/unmapped oracle holes: worsened primary metric and constrained SGP 49x7.
- Lowest-risk candidate promotion within top-three frontier: much worse; disruption risk alone cannot replace contact-opportunity scoring.
- Independent local stay-vs-oracle target filtering: damaged coherent SGP structure and created major SGP regressions.
- Blindly increasing the warmup scaffold budget to 2s: one failure and much higher construction time despite a better non-penalized relative mean.
- Budget-guarded 2s warmup (only for construction budgets >= 4s): no failure but worse primary metric and Sailing.
- Hard-filtering projection to high-attendance people: worsened constrained SGP sentinels; attendance should be a score signal, not a hard eligibility rule.
- Skipping oracle when scaffold has zero raw repeat penalty: too broad; oracle geometry can still help the search basin.
- Requiring dummy-free template candidates when available: caused transfer-attribute failure; negative dummy assignment score also worsened constrained SGP, so dummy burden needs candidate/fill strategy rather than hard filtering or projection pressure.
- Bounded small-oracle multistart selected by pure unique-contact count: regressed broad metric versus the kept 0.20 contact-anchor line; pure-contact oracle quality alone is not a safe downstream construction-basin signal.
- Projection person-priority scalar tuning is exhausted for now: 0.02 and 0.0 both regressed versus the kept 0.01 line.
- Displaced repair pair-pressure anchoring regressed sailing-real and large-gender badly; repair should not preserve scaffold contacts via that simple signal.
- Displaced repair attribute-balance delta scoring also regressed; attribute handling needs projection-level structural mapping rather than late fill scoring.
- Simple bounded projection-level attribute swap polishing also regressed; if revisited, it needs a richer combined contact/attribute objective rather than post-assignment penalty swaps.
- Stronger dummy-oracle candidate penalty regressed like dummy-free filtering/projection pressure; stop scalar dummy tuning.
- Runtime-scaled early stop for the 1s warmup scaffold saved construction time but worsened broad quality; adaptive 1.25s warmup for no-repeat attribute-balance scenarios caused failures. Keep the strict 1s warmup unless a stronger quality signal exists within budget.
- Projection placement anchor 0.35 over-anchored and regressed; keep 0.25 and stop simple projection anchoring scalar tuning.
- Bounded post-merge zero-change candidate retries and group-local no-op preservation during merge both regressed; keep only the cheap pre-merge no-op skip unless telemetry proves zero-change merges are common.
- Shorter pure-structure oracle budgets regressed overall: 250/50 improved Google-CP but hurt primary, and 400/80 was worse; keep 500/100 and stop simple oracle-budget scalar tuning.
- Template group-size ladder width tuning is exhausted: narrowing to max/max-1 and extending to max-3 both regressed; keep max/max-1/max-2.
- General displacing merge in hard-apart sessions with repair regressed; keep conservative non-displacing hard-apart merge.
- Evenly-spaced non-contiguous oracle templates helped transfer-attribute strongly in the ungated run but regressed broad/zero-regression; a coarse partial-participation gate was even worse, so only revisit with precise telemetry/selection.
- Oracle target acceptance keep bonus should stay at 3.0: reducing to 2.0 regressed badly, increasing to 4.0 after original-slot restore improved Google but regressed broad/zero balance, clique-only 4.0 also regressed, retrying 4.0 on the dynamic line fixed the zero guard but still regressed primary to `1.072976464`, combining 4.0 with two projection iterations regressed to `1.118817298`, and suppressing keep only for empty dynamic target groups regressed to `1.139739130`.
- Full selected-region slot assignment over projected candidates and scaffold occupants catastrophically collapsed SGP coherence; any oracle-guided fill must preserve whole oracle groups/cohorts, not independent slots.
- Limiting outside-region oracle imports to genuine open selected capacity regressed; increasing outside-region import penalty after original-slot restore improved Google/mean but returned the zero guard, lowering the penalty eliminated the zero guard but regressed successful-case mean, and combining lower penalty with stable-coverage retuning also regressed; clique-only penalty also regressed, so outside-region imports remain useful but scalar penalty tuning is exhausted.
- Static projection-time attribute-balance signature scoring regressed and increased construction time; late post-merge attribute-balance swap polish also regressed despite improving Google/Sailing; a naive randomized per-session attribute-balanced warmup seed regressed to `1.186993348`; attribute-aware changes need a cheaper and more faithful contact-aware group-fill model.
- Merge-cost/eviction-aware oracle group alignment regressed; basin preservation works better as the kept merge-stage original-slot restore than as projection alignment penalties.
- Extending no-op template skip to non-hard-apart regions with no mutable/open capacity regressed and failed checks; only revisit with telemetry showing this pattern matters.
- Single-pass `score_constructed_schedule` fast path and warmup score reuse did not improve construction time/metric; switching transfer/clique-swap sampling to trusted previews caused one benchmark failure (`1001.070568094`), and transfer-only trusted previews regressed to `1.150170766`; do performance work via dedicated profiling and stronger sampler invariants, not broad-score noise.
- Hard-apart projection pruning by unmapping lower-priority conflicted oracle people improved sailing-real but still regressed broad/constrained balance versus the kept best; projection-only hard-apart swap repair and filtering projection to only merge-actionable/non-frozen people also regressed, so avoid relabeling-only hard-apart/actionability fixes. Frozen/clique anchors appear useful for group alignment even when merge cannot move them.
- Dynamic attribute-balance marginal scoring during oracle target acceptance improved flotilla but regressed the broad metric; a restricted attribute-only target-fill balance score on the dynamic line also regressed to `1.116541981` with the zero guard. Local attribute terms are not enough without a fuller fill redesign.
- Coarse majority-quorum gating, all-or-nothing conflict-free oracle-group acceptance, and a hard nonnegative dynamic target-score cutoff (`1.130060941`) all regressed; skipping partial/low-score imports is too blunt even though cohort preservation remains desirable.
- Do not exclude active-clique sessions from oracle template windows wholesale; it improved Google but catastrophically damaged constrained SGP geometry and broad quality.
- Hard slot ordering by mutable internal contact pressure eliminated the zero-regression guard but hurt weighted successful cases; soft candidate-score, plain-flexible-only, and raw repeat-burden window-ranking variants also regressed, so avoid these signals except as telemetry.
- New diagnostic from the 13x13x14 contact-only + one low-weight `AttributeBalance` case: benchmark scored `2.0` with perfect unique contacts, and a direct oracle solve showed many session-local groups already satisfying the requested `g0/session_0` 6M/7F split. Swapping only the session-0 group labels (`g0` with a matching group such as `g7`) preserved `unique_contacts=14196` and `repetition_penalty=0` while evaluating to final score `0.0`. This points to a generic, non-cheating post-construction per-session group-label assignment/relabeling pass: treat constructed groups as unlabeled buckets and solve a small session-local assignment to official group IDs minimizing group-scoped hard/soft constraint cost before runtime-state validation/search.
