fn main() {
    eprintln!(
        "solver6 seed catalog generation has been retired; solver6 now uses the progressive incumbent cache documented in backend/core/src/solver6/SEED_CATALOG.md"
    );
}
