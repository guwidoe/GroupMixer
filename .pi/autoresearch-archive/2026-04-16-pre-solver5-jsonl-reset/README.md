# Autoresearch archive: 2026-04-16 pre-solver5 jsonl reset

This archive preserves the previous `autoresearch.jsonl` history before resetting
it for the current solver5 construction-coverage autoresearch run.

## Why archived

The working `autoresearch.jsonl` had accumulated a large amount of stale history
from earlier solver4 and transitional runs, which is no longer useful as the
active experiment ledger for the current solver5 constructor-portfolio work.

## Preserved files

- `autoresearch.jsonl`

## Reset intent

After this archive, the root `autoresearch.jsonl` is intentionally reset so the
next solver5 autoresearch loop can start with a clean experiment history and a
fresh config header.
